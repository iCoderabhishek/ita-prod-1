import type { NextConfig } from "next";

const nextConfig: NextConfig = {
 images: {
    domains: ['images.unsplash.com', 'encrypted-tbn0.gstatic.com'], // Add images.unsplash.com to the array
  },
};

export default nextConfig;
